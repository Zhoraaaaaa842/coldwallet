# ColdVault ETH — Зависимости
# Core (криптография + Ethereum)
eth-account>=0.12.0
web3>=6.15.0
mnemonic>=0.21
cryptography>=42.0.0
eth-keys>=0.5.0

# Desktop GUI
PyQt6>=6.6.0

# QR-коды (генерация + распознавание)
qrcode[pil]>=7.4.2
Pillow>=10.0.0
opencv-python>=4.8.0  # для декодирования QR без проблем с DLL

# Build (для создания .exe)
pyinstaller>=6.0.0
