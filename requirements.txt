# ColdVault ETH — Зависимости
# Core (криптография + Ethereum)
eth-account>=0.12.0
web3>=6.15.0
mnemonic>=0.21
cryptography>=42.0.0
eth-keys>=0.5.0

# Desktop GUI
PyQt6>=6.6.0

# Build (для создания .exe)
pyinstaller>=6.0.0
